<configuration>
    <!-- File appender (clears log file at each execution) -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/application.log</file>
        <append>false</append>
        <encoder>
            <pattern>%-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Disable console output completely -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>OFF</level>
        </filter>
    </appender>

    <!-- Set logging level for your application -->
    <logger name="ds" level="OFF" additivity="false">
        <appender-ref ref="FILE" />
    </logger>
    
    <!-- Set logging level for Akka (to reduce noise) -->
    <logger name="akka" level="OFF" additivity="false">
        <appender-ref ref="FILE" />
    </logger>
    
    <!-- Root logger - no output -->
    <root level="OFF">
        <appender-ref ref="FILE" />
    </root>
</configuration>
